# Project State — App Launcher (GitHub Repos Aggregator)

## Current Focus
האפליקציה הוחלפה מרשימת קניות למסך "אפליקציות" שמאגד את כל הריפוזיטוריז הציבוריים מ-GitHub (`ronmailx-boop`) כאייקונים לחיצים, בהשראת מסך Apps של כרום באנדרואיד. הרשימה נטענת דינמית מול `api.github.com` בכל טעינת עמוד (ללא hardcoding), כך שריפו חדש מופיע אוטומטית. לכל אייקון יש ניסיון טעינת favicon מה-GitHub Pages של הריפו, עם נפילה חזרה לאריח צבעוני עם האות הראשונה. לחיצה על אייקון מובילה ל-GitHub Pages של אותו ריפו. הכל עלה ל-`claude/github-repos-aggregator-bi0jjv` וממתין לפריסה.

האתר עלה ל-main ופורסם, אבל המשתמש דיווח שהמסך נתקע לצמיתות על "טוען אפליקציות..." בנייד. הסיבה: לבקשת ה-fetch מול GitHub API לא היה timeout, אז אם הבקשה נתקעת/איטית (חסימת רשת סלולרית וכו') הספינר נשאר תלוי לנצח בלי הודעת שגיאה. תוקן: נוסף AbortController עם timeout של 10 שניות, והודעת שגיאה ספציפית למקרה של חריגת מכסת בקשות (403). עלה ל-`claude/fix-loading-hang` וממתין למיזוג.

האפליקציות עלו לטעון בהצלחה, אבל כל האייקונים הופיעו כאריחי-אות גנריים (fallback) במקום הלוגו האמיתי של כל ריפו — כי הקוד ניחש רק `favicon.ico` בשורש, וזו כמעט אף פעם לא הדרך שבה אתרים מודרניים/PWA מגדירים את האייקון שלהם. תוקן: נוספה `resolveRepoIcon()` שבודקת (באותו סדר שדפדפן משתמש בו ל"הוספה למסך הבית") — Web App Manifest (`icons[]`, הכי גדול), ואז `apple-touch-icon`, ואז `icon`/`shortcut icon`, ורק לבסוף נופלת ל-`favicon.ico`/אריח-אות. אפשרי כי כל הריפואים תחת אותו host (`ronmailx-boop.github.io`) נחשבים same-origin, אז אין חסימת CORS ב-fetch של HTML/manifest של ריפו אחר. עלה ל-`claude/repo-icon-resolution` וממתין למיזוג.

הלוגואים האמיתיים אושרו ע"י המשתמש כעובדים מצוין. בקשה חדשה: לוגו ייעודי לאפליקציית האגרגטור עצמה (my-site) שיופיע כשמתקינים אותה בכרום באנדרואיד, אפשרות התקנה בפועל, והסרת my-site מרשימת הריפואים (כי היא האפליקציה עצמה). בוצע: נוצר לוגו (svg → PNG ב-192/512, גריד 2x2 בצבעי האפליקציה על רקע הגרדיאנט הכהה של האתר, ב-`icons/`), נוסף `manifest.json` (name/icons/start_url/display:standalone/theme_color) עם קישור מ-`index.html` (`link rel="manifest"` + `icon` + `apple-touch-icon` + `meta theme-color`) — זה מה שגורם לכרום להציג "התקן אפליקציה". וב-`app.js` נוסף סינון `SELF_REPO = 'my-site'` כך שהריפו של האתר עצמו לא מופיע ברשימה. עלה ל-`claude/app-icon-and-manifest` וממתין למיזוג.

**הצעד הבא (אם ירצה):** לוודא בדפדפן אמיתי של המשתמש שאפשרות "התקן אפליקציה" מופיעה בתפריט כרום, ושהלוגו החדש מוצג נכון אחרי ההתקנה.

## Checklist
- [x] index.html: מסך "האפליקציות שלי" (RTL, Tailwind CDN, רקע כהה, מצבי טעינה/שגיאה/ריק)
- [x] app.js: fetch דינמי מול GitHub API (`/users/{username}/repos`)
- [x] app.js: סינון forks
- [x] app.js: רינדור גריד אייקונים + קישור ל-GitHub Pages של כל ריפו
- [x] app.js: fallback לאייקון (אות ראשונה + צבע לפי hash) כשאין אייקון אמיתי
- [x] טיפול בשגיאות רשת עם הודעה בעברית + כפתור נסה שוב
- [x] README.md עודכן לתיאור הפרויקט החדש
- [x] fix: timeout ל-fetch (10s) כדי שספינר "טוען" לא יתקע לנצח על רשת איטית/חסומה
- [x] fix: הודעת שגיאה ספציפית לחריגת מכסת בקשות GitHub (403)
- [x] fix: משיכת האייקון האמיתי של כל ריפו (manifest / apple-touch-icon / icon) במקום ניחוש favicon.ico בלבד
- [x] לוגו ייעודי לאפליקציה + manifest.json + קישורים ב-index.html לתמיכה ב"התקן אפליקציה" בכרום
- [x] app.js: סינון my-site מרשימת הריפואים (זו האפליקציה עצמה)
- [ ] אימות ידנית של המשתמש שאפשרות ההתקנה מופיעה ושהלוגו נכון אחרי התקנה
